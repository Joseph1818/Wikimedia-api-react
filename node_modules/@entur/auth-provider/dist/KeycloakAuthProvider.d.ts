import React from 'react';
interface Props {
    children: React.ReactNode;
    configUrl?: string;
}
export declare const KeycloakAuthProvider: ({ children, configUrl }: Props) => JSX.Element;
export {};
