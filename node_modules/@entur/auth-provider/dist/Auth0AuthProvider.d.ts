import React from 'react';
interface Props {
    children?: React.ReactNode;
    claimsNamespace?: string;
    loginAutomatically?: boolean;
}
export declare const Auth0AuthProvider: ({ children, claimsNamespace, loginAutomatically }: Props) => JSX.Element;
export {};
